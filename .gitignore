# ATC 转换过程中产生的临时文件 / 日志
kernel_meta/
fusion_result.json
*.log

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# 可视化输出（可随时重新生成）
results/pt_result/
results/om_result.jpg
logs/*.npy
logs/*.npz

# 模型产物一律不入库 —— 不只是体积问题：
#   yolov8n.pt / .onnx / .om 都源自 Ultralytics 的预训练权重（AGPL-3.0），
#   本仓库以 MIT 发布，因此不分发这些文件。
#   运行 python scripts/run_all.py 会自动从 Ultralytics 官方发布页下载权重，
#   并在本地生成 onnx / om。
model/*.pt
model/*.onnx
model/*.om
